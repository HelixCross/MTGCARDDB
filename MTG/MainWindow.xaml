<Window x:Class="MTG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MTG"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        
        mc:Ignorable="d" WindowStartupLocation="CenterScreen" Background="#FF2D2D2D"
        Title="MTG Card Catalogue" Height="620" Width="1200"
        >
    <Window.Resources>
        <!-- Modern TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2C2C2C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="14"/>

        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3A86FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="4"/>

            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Color="#222" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00353B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2F6CD0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Top Menu -->
        <Menu DockPanel.Dock="Top" Background="#252525" Foreground="Black">
            <MenuItem Header="_File">
                <MenuItem Header="_Save" Click="Click_Save"/>
                <MenuItem Header="_Update" Click="Click_Update"/>
                <MenuItem Header="_Delete" Click="Click_Delete"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Click_Exit"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="Cut"/>
                <MenuItem Header="Copy"/>
                <MenuItem Header="Paste"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="About" Click="Click_Help"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Background="#FF282828" Foreground="White" Height="50">
            <StatusBarItem>
                <TextBlock x:Name="lblTotalCards" Text="Ready" VerticalAlignment="Center"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <Button Content="Update" Style="{StaticResource ModernButton}" Width="80"  Height="30" Click="Button_Click"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Card Entry -->
            <StackPanel Grid.Column="0" Background="#2B2B2B"  Margin="5" >
                <TextBlock Text="Card Information" FontSize="16" FontWeight="Bold" Foreground="#5CA8FF" Margin="0,0,0,10"/>

                <TextBlock Text="Card Name:" Foreground="White"/>
                <TextBox x:Name="txtName"/>

                <TextBlock Text="Colour:" Foreground="White"/>
                <TextBox x:Name="txtColour"/>

                <TextBlock Text="Rarity:" Foreground="White"/>
                <TextBox x:Name="txtRarity"/>

                <TextBlock Text="Mana Cost:" Foreground="White"/>
                <TextBox x:Name="txtManaCost"/>

                <TextBlock Text="Type:" Foreground="White"/>
                <TextBox x:Name="txtType"/>

                <TextBlock Text="Edition:" Foreground="White"/>
                <TextBox x:Name="txtEdition"/>

                <TextBlock Text="Quantity:" Foreground="White"/>
                <TextBox x:Name="txtQuantity"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Clear Fields" Width="100" Click="Click_Clear" Style="{StaticResource ModernButton}"/>
                </StackPanel>
            </StackPanel>

            <!-- Right: Search + DataGrid -->
            <DockPanel Grid.Column="1" Margin="10,0,0,0">
                <!-- Search Bar -->
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBox x:Name="txtSearch" Width="250" Height="28" VerticalContentAlignment="Center"
                             ToolTip="Search cards by name or field." />
                    <Button Content="Search / Reload DB" Width="150" Click="Click_Search" Style="{StaticResource ModernButton}"/>
                </StackPanel>

                <!-- DataGrid -->
                <DataGrid x:Name="dataGrid" DockPanel.Dock="Top"
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="dataGrid_SelectionChanged"
                          Background="#2C2C2C"
                          Foreground="black"
                          BorderBrush="#3A3A3A"
                          BorderThickness="1"
                          RowBackground="#333"
                          AlternatingRowBackground="#3C3C3C"
                          Height="400"
                          Margin="0,5,0,0"/>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>